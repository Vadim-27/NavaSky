import { Box } from '@mui/material';
import OurServices from '@/components/OurServices';

const HomePage = () => {
    return (
        <Box component="main">
            <OurServices />
        </Box>
    );
};

export default HomePage;
